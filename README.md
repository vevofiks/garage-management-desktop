# Garage Management System

Offline-first garage management & billing desktop app for Babu Awamir Auto Garage (Qatar) — Next.js (App Router) for both frontend and backend, wrapped in Electron, with a local SQLite database as the offline source of truth.

Full requirements: [`docs/prd.md`](docs/prd.md). Build plan / progress checklist: [`docs/implementation.md`](docs/implementation.md). Original environment walkthrough: [`docs/setup.md`](docs/setup.md).

## Prerequisites

- **Node.js 20+** (`node -v` to check). `npm install` will print `EBADENGINE` warnings about `@electron/rebuild`/`node-abi` wanting Node 22 — safe to ignore, everything works on 20.x.
- npm (ships with Node)
- A Windows machine eventually for testing the packaged installer (not needed for day-to-day dev — the app runs fine on macOS/Linux in dev mode).

## 1. Clone and install

```bash
git clone <repo-url>
cd garage-management-system
npm install
```

## 2. Environment variables (optional)

```bash
cp .env.example .env.local
```

Only one variable exists right now, and it's optional:

| Variable | Required? | What it's for |
|---|---|---|
| `SHADCNIO_TOKEN` | No | Powers the `shadcnio` MCP server (see [Adding shadcn/ui components](#adding-shadcnui-components) below). The app and every existing feature work fully without it. |

If you skip this, just don't `cp .env.example .env.local` — there's nothing else required to run the app.

## 3. Run the app

```bash
npm run dev
```

This starts the Next.js dev server **and** opens it in an Electron window together, with hot reload. First run:

- Creates `data/garage.db` automatically (a local SQLite file, gitignored — every developer has their own).
- The app redirects to `/login`. You need an admin account before you can do anything — see the next step.

Other dev scripts, if you only need one piece:

```bash
npm run dev:next      # Next.js dev server only, at http://localhost:3000
npm run dev:electron  # Electron only — waits for localhost:3000, then opens the window
npm run build          # next build
npm run start           # next start (production server, no Electron)
npm run lint              # eslint
```

There's no test runner configured yet.

## 4. Create your first admin account

There's no sign-up UI by design (only an Admin creates other accounts, per the PRD). Seed the first one with the provided script:

```bash
node scripts/create-admin.js <username> <password>
# e.g.
node scripts/create-admin.js admin changeme
```

Run it again with a different username any time you need another admin, or use the app's own **Users** page (admin-only) once you're logged in to add `staff` accounts.

## Tech stack

| Layer | Choice |
|---|---|
| Frontend + backend | Next.js 16 (App Router) — UI in `src/app/**/page.tsx`, API in `src/app/api/**/route.ts` |
| Desktop shell | Electron (`electron/main.js`, `electron/preload.js`) — a thin window pointed at the Next.js server, no business logic of its own |
| Local database | SQLite via `better-sqlite3`, WAL mode, schema bootstrapped in `src/lib/db.ts` (no separate migration system — edit the `db.exec(...)` block directly) |
| Server state | TanStack Query (`@tanstack/react-query`) — see [`docs/implementation.md`](docs/implementation.md#frontend-architecture-state--data-consistency) for the query-key + invalidation conventions |
| Forms/validation | `react-hook-form` + `zod`, schemas shared between client forms and API route handlers (`src/lib/schemas/*.ts`) |
| UI components | [shadcn/ui](https://ui.shadcn.com) (this project's version builds on `@base-ui/react`, not Radix — see the note below if something you remember about shadcn doesn't match) |
| Auth | Cookie session (`src/lib/auth.ts`), no external auth library — single-machine desktop app |

## ⚠️ This is not the Next.js (or shadcn) you remember

Both Next.js 16 and this project's `shadcn` CLI version are newer than most people's — and any AI assistant's — training data, with real breaking changes. Two that already bit us once (see `docs/implementation.md`'s Phase 1 notes for the full story):

- **`middleware.ts` → `proxy.ts`.** Next.js 16 renamed the file, the export (`proxy`, not `middleware`), and — for a `src/` layout — the required location (`src/proxy.ts`, not the project root). The old convention is silently never invoked; there's no error, no warning, it just doesn't run. If you're adding proxy/middleware logic, start from `src/proxy.ts`.
- **shadcn's `Button` defaults to `nativeButton: true`.** If you compose `<Button render={<Link .../>} />` to make a styled link, pass `nativeButton={false}` or Base UI logs a console error and drops native button semantics.

Before writing code that touches Next.js internals (route handlers, caching, `cookies()`/`headers()`, proxy/middleware, etc.), skim `node_modules/next/dist/docs/` — `AGENTS.md` at the repo root explains why and is auto-regenerated by `next dev`, so don't fight it if it reappears in a diff.

## Adding shadcn/ui components

Preferred path (once set up): ask your AI agent to *"use shadcnio to install `<component-name>`"* — this goes through the `shadcnio` MCP server (already registered in the committed `.mcp.json`), which searches/previews the live registry and asks for approval before writing files.

To unlock it:
1. Get a shadcn.io Pro account token from your shadcn.io account settings.
2. `cp .env.example .env.local` and set `SHADCNIO_TOKEN=<your token>` in `.env.local` (never commit the real token — `.env.local` is gitignored).
3. `set -a; source .env.local; set +a` in your shell (or `export SHADCNIO_TOKEN=...` directly), so the variable is in your shell's environment.
4. Start (or restart) your `claude` session from this directory, and approve the `shadcnio` server when prompted — new project-scoped MCP servers require one-time approval. Check status any time with `claude mcp get shadcnio`.

Fallback, if you haven't set that up yet: `npx shadcn@latest add <component-name>`.

## Project structure

```
electron/           Electron main process + preload script (no business logic)
src/
  app/
    api/**/route.ts   Backend — all business logic and DB access lives here
    **/page.tsx        Frontend pages (App Router)
    proxy.ts            Auth redirect for unauthenticated requests (see the warning above)
  components/ui/       shadcn/ui components (generated — see "Adding shadcn/ui components")
  hooks/                Shared client hooks (e.g. useAuth, useRequireRole)
  lib/
    db.ts               SQLite connection + schema bootstrap — server-only, never import from a Client Component
    schemas/            zod schemas shared by API routes and client forms
    query-keys.ts        TanStack Query key factory
    api-client.ts         Typed fetch wrapper used by every query/mutation
scripts/create-admin.js  Seeds the first admin account (see step 4 above)
docs/                   PRD, environment setup notes, and the phase-by-phase build plan
data/                   Local SQLite database (gitignored, created on first run)
```

## Where to go next

- **What's built vs. still planned:** [`docs/implementation.md`](docs/implementation.md) is a phase-by-phase checklist (`- [ ]` / `- [x]`) — check it before starting work so you don't duplicate what another teammate (or agent) already built, and tick items off as you go.
- **Full product requirements:** [`docs/prd.md`](docs/prd.md).
